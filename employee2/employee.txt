show databases;

create database emp;

use emp;

create table employees(
	id int not null auto_increment,
	userid varchar(20),
	passwd varchar(20),
	username varchar(10),
	regdate datetime,
	addr varchar(100),
	primary key(id)
);

desc employees;





